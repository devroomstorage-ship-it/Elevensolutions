export const metadata = {
  title: 'Staff Portal — Eleven Solutions Limited',
  robots: 'noindex, nofollow',   // Never index the admin portal
};

export default function PortalLayout({ children }) {
  return children;
}
